<%= form_for(@property) do |f| %>
  <% if @property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property.errors.count, "error") %> prohibited this property from being saved:</h2>

      <ul>
      <% @property.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lot_size %><br />
    <%= f.text_field :lot_size %>
  </div>
  <div class="field">
    <%= f.label :propertible_id %><br />
    <%= f.number_field :propertible_id %>
  </div>
  <div class="field">
    <%= f.label :propertible_type %><br />
    <%= f.text_field :propertible_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
